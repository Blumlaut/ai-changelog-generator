{
  "name": "ai-changelog-generator",
  "version": "1.1.0",
  "description": "GitHub Action to generate changelog entries using AI",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "bundle": "npm ci && npx ncc build index.js -o dist",
    "test": "node --test"
  },
  "dependencies": {
    "@actions/core": "^2.0.3",
    "@actions/github": "^9.0.0",
    "@actions/http-client": "^4.0.0",
    "ignore": "^7.0.5"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
